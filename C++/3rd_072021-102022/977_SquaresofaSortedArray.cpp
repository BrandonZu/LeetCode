//
// Created by BrandonZu on 11/27/21.
//

#include "common.h"

class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        int n = nums.size();
        int l = 0, r = n - 1;
        while(l < r) {
            int mid = l + r >> 1;
            if()
        }
    }
};
